<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="alfialdo.jwork_android">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Jworkandroid"
        android:usesCleartextTraffic="true">
        <activity android:name=".activity.InfoPageActivity"></activity>
        <activity android:name=".activity.AddBonusActivity" />
        <activity android:name=".activity.AddRecruiterActivity" />
        <activity android:name=".activity.AdminPageActivity" />
        <activity android:name=".activity.AddJobActivity" />
        <activity android:name=".activity.FinishedJobActivity" />
        <activity android:name=".activity.ApplyJobActivity" />
        <activity android:name=".activity.MainActivity" />
        <activity android:name=".activity.RegisterActivity" />
        <activity android:name=".activity.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>